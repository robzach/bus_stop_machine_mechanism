ballDiameter = 5;
linkLength = 1;
numTeeth = 50;

circleRadius = numTeeth * ballDiameter;

difference(
circle(circleRadius);
for(int i = 0; i < numTeeth; i++){
    translate(circleRadius
    circle(
}
)

